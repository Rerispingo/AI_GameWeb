/**
 * Configuração de tema para o Tailwind CSS.
 * Define as cores customizadas utilizadas no projeto.
 */
tailwind.config = {
    theme: {
        extend: {
            colors: {
                sidebar: '#222',
                resizer: '#444',
                'resizer-hover': '#666',
                toggle: '#555',
            }
        }
    }
}
